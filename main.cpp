#include <iostream>
#include "menu.hpp"
#include "tests.hpp"

int main() {
    tests();
    menu();
//    BinaryTree<int> *tree = nullptr;
//    tree->addNode(tree, 2);
//    tree->addNode(tree, 1);
//    tree->addNode(tree, 3);
//    std::string *ans = new std::string ();
//    tree->printToString(tree, ans);
//    std::cout << ans[0] << std::endl;
//    return 0;
}
